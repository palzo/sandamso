<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SanSanInfo" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:textColor">@color/grey_700</item>
        <item name="android:textColorHint">#808080</item>
    </style>

    <style name="CustomText">
        <item name="android:textAllCaps">true</item>
        <item name="android:fontFamily">serif</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">italic|bold</item>
    </style>

    <style name="Theme.SanSanInfo" parent="Base.Theme.SanSanInfo" />
    <style name="PowerSpinnerStyle">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">false</item>
        <item name="android:scrollbarSize">5dp</item>
        <item name="android:scrollbarThumbVertical">@drawable/powerspinner_bar</item>
    </style>
</resources>